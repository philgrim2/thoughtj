package live.thought.thoughtj.evolution.listeners;

import live.thought.thoughtj.evolution.EvolutionUser;

public interface EvolutionUserRemovedEventListener {

    void onUserRemoved(EvolutionUser user);
}